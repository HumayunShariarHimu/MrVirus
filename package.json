{
  "name": "password-to-telegram",
  "version": "1.0.0",
  "private": true,
  "description": "Export password CSV → your own Telegram bot",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "engines": {
    "node": ">=18"
  }
}
